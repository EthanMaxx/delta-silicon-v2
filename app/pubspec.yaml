name: delta_silicon
description: Offline AI diabetes companion – zero cloud, zero rupee
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  camera: ^0.10.6
  path_provider: ^2.1.3
  permission_handler: ^11.3.1
  flutter_tts: ^3.8.5
  shared_preferences: ^2.3.3
  http: ^1.2.1
  onnxruntime: ^1.3.0
  flutter_localizations:
    sdk: flutter
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/
    - assets/docs/
    - assets/l10n/